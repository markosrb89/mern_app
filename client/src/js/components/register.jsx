import React from "react";

import Page from "./common/page";

const Register = props => {
    return (
        <Page>
            <section className="register">
                Register -> UNDER DEVELOPMENT
            </section>
        </Page>
    );
}

export default Register;